<%= form_for(@ticket) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :body %>
  <%= f.text_field :body %><br>

  <%= f.label :due_date %>
  <%= f.date_field :due_date %><br>

<% end %>
